# Prism Frontend Environment Variables
# Note: Frontend communicates with backend server, so no AI API keys needed here

# API Configuration
VITE_API_BASE_URL=http://localhost:3001

# Optional: Custom development settings (uncomment if needed)
# VITE_DEBUG_MODE=true